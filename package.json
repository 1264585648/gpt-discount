{
  "name": "gpt-discount",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "audit:links": "node scripts/audit-links.mjs",
    "audit:build": "node scripts/audit-build.mjs",
    "verify": "npm run check && npm run audit:links && npm run build && npm run audit:build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.1",
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.3"
  }
}
